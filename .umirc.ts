import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Library Name',
  // more config: https://d.umijs.org/config
  // 文档模式（默认值）
  //  mode: 'doc',
  // 站点模式
  // mode: 'site',
});
